{
  "name": "dynamodb-demo",
  "version": "1.0.0",
  "description": "# Dynamodb local initialize",
  "main": "index.js",
  "scripts": {
    "local-init": "node src/index.js",
    "aws-push": "node src/index.js aws",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.460.0",
    "dotenv": "^8.0.0",
    "uuid": "^3.3.2"
  }
}
